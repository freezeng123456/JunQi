# JunQi observation features and network depth, 2026-09-21

Two additional user-authorized servers, one idle H20 per server. F explores an observation-only tactical residual (material proxy, entropy, flag/mine/bomb beliefs); D extends Transformer depth 4 to 6. Source is a committed isolated worktree based on 2cae8a9efa555a1de618d95b71826fd1dc3ce5c9. No environment/CUDA observation schema change, no hidden identities, no reward shaping, no EMA, no belief or arrangement training.

Shared start: full B_extended 26144 checkpoint, SHA256 1d03332795e9961d70c5760bb1884df02a5932d7a63428c9873321f4c02ebdc7, 2509867 optimizer updates. F zeroes its new output layer; D zeroes the two new blocks' attention output and FFN output projections. All inherited policy tensors and AdamW moments mapped by parameter name must remain exactly equal. New parameters have no inherited moments. CPU FP32/BF16 and GPU behavior checks precede formal training. Smoke weights are discarded.

Both arms keep LR1e-5, alpha .006, env128, rollout512, BF16, 1 PPO epoch, advantage keep .75. Three 2048-rollout blocks to primary absolute32288 (6144 new); seeds9201/9202/9203, environment reset per block, exactly matching the existing LR experiment A control. A fourth block with seed9204 to34336 (8192 new) is an extra-budget result, never substituted for the primary based on scores. Only one GPU child per server. Preserve all numbered checkpoints, configs, logs, migration evidence, source, and comparison rows.

Training hard cutoff 2026-09-21 19:30 Asia/Shanghai plus180s graceful stop. Evaluation cutoff22:30; final upload by23:30; reclaim23:59. At most8192 formal new rollouts per server; no extension beyond lease. Save128, monitor1024 using existing monitor suite31920000/32920000 with256 games. HF checkpoint audit backup every2h and each block. The primary comparison is fixed at32288, not selected on monitoring/heldout. If deadline prevents reaching primary, report partial and retain the latest valid model without substituting a biased comparison.

Independent fresh paired2048-game suites per arm vs control32288 and start26144; F also vs D32288. Setup seeds41921000/game42921000. Random512 games43921000/44921000. Extra checkpoint34336 may be compared to control34336 (new seeds45921000/46921000), explicitly a separate training budget. Predeclare single-seed exploratory scope, five primary directional comparisons, no familywise significance claim from one nominal95% interval. Report W/L/D, pure win, half-draw score, paired bootstrap95% with10000 resamples, walltime and parameter counts. Direct ranking is exploratory; confirmation across training seeds remains future work.

F material proxy by type: flag1, mine.3, bomb.6, engineer through commander1/9..1. These are network inputs, not altered rewards or externally established piece valuations. Existing combat-probability head stays enabled. New9-feature32-hidden-unit residual outputs only attack logits and uses actor observation only.

Control primary downloaded from existing A/stage_3, same base, seeds, training blocks, hyperparameters and full optimizer; no added control training on the old server. F and D remain separate candidates and are not automatically promoted. Private HF a1390892757/junqi-guarded-continue-20260914 prefix explore_20260921; GitHub results/explore-20260921. Publication must retain negative/partial evidence. Completion requires cloud receipts, manifests, local recovery and strict reload.
