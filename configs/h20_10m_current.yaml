# Current 10.8M H20 self-play profile.
#
# Keep every model, rollout, optimiser, entropy/KL and arrangement setting in
# sync with the Ataraxos self-play recipe. The only deliberate difference is
# that every transition is retained instead of selecting an advantage
# quantile. Machine-specific resume/output paths remain command-line options.

extends: ataraxos_selfplay.yaml

ppo:
  adv_filt_rate: 1.0
