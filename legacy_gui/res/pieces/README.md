# 原始棋子资源说明

恢复分支的原生 GTK 客户端使用 `legacy_gui/res` 下原始的四条横向棋子资源：
`orange.bmp`、`purple.bmp`、`green.bmp` 和 `blue.bmp`。

`res/pieces` 下的逐枚裁剪资源是之前一次实验留下的兼容资产，不是当前原生客户端的主加载路径。保留它们是为了不破坏旧安装包和历史文件；如果需要复原原始客户端视觉效果，应使用 `res` 根目录的四条颜色棋子条。
